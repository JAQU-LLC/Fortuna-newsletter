node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables
# .env is committed (for local development)
# Only ignore local override files (contain secrets)
.env.local
.env.*.local